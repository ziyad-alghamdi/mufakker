# 🎨 دليل الهوية البصرية الموحّدة - Mufakker Platform

## 📋 نظرة عامة
تم توحيد جميع صفحات المنصة لتتبع نفس الهوية البصرية المستوحاة من القائمة الجانبية.

---

## 🎨 الألوان الرسمية (استخدم هذه الألوان فقط)

### الألوان الأساسية:
```css
--navy-dark: #0b2a41;      /* كحلي داكن - الخلفية الأساسية */
--petrol-blue: #004e64;    /* بترولي - الخلفيات الثانوية */
--teal: #25a18e;           /* تركوازي - الأزرار والعناوين النشطة */
--light-teal: #47d6ad;     /* تركوازي فاتح - العناوين والتفاصيل */
--white-blue: #eafff9;     /* أبيض مائل للأزرق - النصوص */
```

### التدرجات:
```css
/* خلفية الصفحات */
background: linear-gradient(135deg, #0b2a41 0%, #004e64 100%);

/* القائمة الجانبية */
background: linear-gradient(180deg, #0b2a41, #004e64);
```

---

## 📐 التخطيط (Layout)

### البنية الأساسية لكل صفحة:
```tsx
<div className="page-container">
  <Sidebar />
  
  <div className="content">
    <h1 className="title">عنوان الصفحة</h1>
    <p className="subtitle">وصف قصير</p>
    
    {/* المحتوى */}
  </div>
</div>
```

### المسافات القياسية:
- **Padding الصفحة**: `50px 30px`
- **Margin من السايدبار**: `margin-right: 270px`
- **Max-width للمحتوى**: `900px` (صفحات عادية) أو `1200px` (صفحات بكروت متعددة)

---

## 🎴 الكروت (Cards)

### التصميم الموحّد:
```css
.card {
  background: rgba(0, 78, 100, 0.5);
  backdrop-filter: blur(10px);
  border: 1px solid rgba(37, 161, 142, 0.3);
  border-radius: 16px;
  padding: 24px;
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-4px);
  border-color: rgba(71, 214, 173, 0.6);
  box-shadow: 0 8px 24px rgba(71, 214, 173, 0.15);
}
```

---

## 🔘 الأزرار (Buttons)

### الزر الأساسي (Primary):
```css
.primary-btn {
  background: #25a18e;
  color: #fff;
  font-weight: 700;
  padding: 14px 20px;
  border-radius: 10px;
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
}

.primary-btn:hover {
  background: #47d6ad;
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(37, 161, 142, 0.3);
}
```

### الزر النشط (Active):
```css
.active-btn {
  background: #25a18e;
  color: #0b2a41;
  box-shadow: 0 4px 12px rgba(37, 161, 142, 0.3);
}
```

---

## 📝 النصوص (Typography)

### العناوين:
```css
h1 {
  font-size: 40px;
  font-weight: 900;
  color: #47d6ad;
  text-align: right;
}

h2 {
  font-size: 28px;
  font-weight: 700;
  color: #47d6ad;
}

h3 {
  font-size: 22px;
  font-weight: 700;
  color: #47d6ad;
}
```

### النصوص العادية:
```css
p {
  font-size: 16-20px;
  color: #eafff9;
  line-height: 1.7-1.8;
  text-align: right;
}
```

---

## 📱 Responsive Design

### Breakpoints:
```css
/* Desktop: default */

/* Tablet: 1100px */
@media (max-width: 1100px) {
  .content {
    margin-right: 0;
    padding: 40px 20px;
  }
}

/* Mobile: 768px */
@media (max-width: 768px) {
  .content {
    padding: 30px 16px;
  }
  
  .title {
    font-size: 32px;
  }
}
```

---

## ✅ الصفحات الموحّدة

تم توحيد التصميم في الصفحات التالية:

1. ✅ **Dashboard** (`/dashboard`) - صفحة الملف الشخصي
2. ✅ **About** (`/about`) - من نحن
3. ✅ **Workshops** (`/workshops`) - الورش والدورات
4. ✅ **Events** (`/events`) - الفعاليات
5. ✅ **Certificates** (`/certificates`) - الشهادات
6. ✅ **Sidebar** - القائمة الجانبية (المرجع الأساسي)

---

## 🚫 ممنوعات التصميم

❌ **لا تستخدم:**
- ألوان خضراء (#00ff88 أو مشابهة)
- خلفيات سوداء صريحة (#000)
- ألوان رمادية
- أبيض صافي (#fff) للخلفيات
- تدرجات خضراء
- Shadow قوي جداً
- ألوان إضافية خارج الهوية

✅ **استخدم فقط:**
- الألوان الأربعة الرسمية
- التدرج من الكحلي إلى البترولي
- RTL كامل
- خط Cairo

---

## 📦 الملفات المحدّثة

```
app/
├── globals.css          ← ملف CSS عام موحّد
├── dashboard/page.tsx   ← صفحة الملف الشخصي
├── about/page.tsx       ← من نحن
├── workshops/page.tsx   ← الورش والدورات
├── events/page.tsx      ← الفعاليات
├── certificates/page.tsx ← الشهادات
└── components/
    └── Sidebar.tsx      ← القائمة الجانبية (المرجع)
```

---

## 🎯 النتيجة النهائية

✅ **تجربة مستخدم متناسقة 100%**
✅ **نفس الألوان في كل الصفحات**
✅ **نفس التدرج والإحساس العام**
✅ **RTL كامل**
✅ **Responsive على جميع الأجهزة**
✅ **بدون ألوان إضافية**
✅ **بدون فراغات على الجوانب**

---

تم التوحيد بنجاح! 🎉
